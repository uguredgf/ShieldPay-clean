[package]
name = "asp_contract"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { version = "21.5.0", features = ["contracts"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
