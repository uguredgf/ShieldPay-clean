[package]
name = "pool_contract"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "26"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true